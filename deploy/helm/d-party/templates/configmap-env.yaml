apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "d-party.configName" . }}
  labels:
    {{- include "d-party.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
